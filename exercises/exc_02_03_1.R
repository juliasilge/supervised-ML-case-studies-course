library(tidyverse)
stackoverflow <- read_csv("data/stackoverflow.csv")

# Print stackoverflow
glimpse(___)

# First count for Remote
stackoverflow %>% 
    count(___, sort = TRUE)

# then count for Country
stackoverflow %>% 
    ___(___, sort = TRUE)
